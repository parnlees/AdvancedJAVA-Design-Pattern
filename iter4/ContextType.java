public enum ContextType {
    // ประกาศ enum ที่มีค่าเป็น NORMAL, REVERSE
    // enum คือ การประกาศค่าคงที่ที่เป็นชนิดข้อมูลเดียวกัน
    // ใช้เพื่อเป็นการเปลี่ยนค่าตามเงื่อนไขที่กำหนด
    NORMAL,
    REVERSE
}
